- task: Bash@3
  displayName: "🔍 Debug: Locate locks.json"
  inputs:
    targetType: 'inline'
    script: |
      echo "🔍 Searching for locks.json under $(Pipeline.Workspace)/locks_artifact..."
      find $(Pipeline.Workspace)/locks_artifact -type f -name "locks.json"
